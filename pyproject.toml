[project]
name = "image-renaming-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "panel>=1.8.10",
    "pathlib>=1.0.1",
]
